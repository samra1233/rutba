rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The public storefront and admin panel use the authenticated server API.
    // All privileged Firestore access is performed by Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
